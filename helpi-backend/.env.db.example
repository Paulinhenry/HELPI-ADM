# Variáveis do container PostgreSQL (usadas pelo docker-compose)
# Copie este arquivo para .env.db e preencha os valores reais
POSTGRES_USER=helpi_user
POSTGRES_PASSWORD=sua_senha_segura_aqui
POSTGRES_DB=helpi_db

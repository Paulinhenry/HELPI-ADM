name: helpi_admin_vault
description: "Helpi Admin Vault - Painel de Backoffice"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: ^0.70.2
  font_awesome_flutter: ^11.0.0
  provider: ^6.1.2
  google_fonts: ^6.2.1
  intl: ^0.20.3
  http: ^1.6.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
